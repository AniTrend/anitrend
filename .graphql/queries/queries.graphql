query ConfigQuery {
    config {
        settings {
            analyticsEnabled
        }
        defaultImage {
            banner,
            default,
            error,
            info,
            loading,
            poster
        }
    }
}

query HomeQuery {
    home {
        genres {
            image,
            mediaId
        }
    }
}