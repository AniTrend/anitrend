query ConfigQuery {
    config {
        settings {
            analyticsEnabled
            platformSource
        }
        image {
            banner,
            default,
            error,
            info,
            loading,
            poster
        }
    }
}